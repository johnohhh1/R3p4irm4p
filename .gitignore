# built output — these carry real site photos, keep them out of the repo
*.pdf
map.html
sample-map.html
state.json
pins.json
.repairmap_tmp/
examples/sample/
__pycache__/
*.pyc
.DS_Store

# local Word copy of the PRD - the markdown is the source of truth
docs/*.docx

# real floor plans are the chain's property - never commit one (PRD SS10)
examples/*.png
examples/*.jpg
examples/*.jpeg
examples/*.webp

# site exports carry real plans and photos
*.rmap
